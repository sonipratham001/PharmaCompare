{
  "name": "pharmacompare",
  "version": "1.0.0",
  "scripts": {
    "start": "npm run backend && npm run frontend",  
    "backend": "npm --prefix pharma-compare-backend start",  
    "frontend": "npm --prefix pharma-compare-frontend run preview", 
    "install-all": "npm install --prefix pharma-compare-backend && npm install --prefix pharma-compare-frontend",
    "build": "npm install --prefix pharma-compare-backend && npm --prefix pharma-compare-frontend run build"  
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {}
}